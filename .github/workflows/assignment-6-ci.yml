name: CI Assignment 6

on:
  pull_request:
    types:
      - opened
    paths:
      - 'packages/assignment-6/**'
  push:
    branches:
      - "**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: job1 구현
        run: |
          cd packages/assignment-6
          npx tsc
          npm run build
        if: failure()
      - name: Run TypeScript compiler and Build
        id: build
        run: |
          output=$(npx tsc 2>&1 && npm run build 2>&1) || echo "::set-output name=error::$output"


  lint:
    runs-on: ubuntu-latest
    steps:
      - name: job2 구현
        run: | 
          npm ci
          npm run lint

  test:
    runs-on: ubuntu-latest
    steps:
      - name: job3 구현
        run: echo "job3의 내용을 구현해주세요"

  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - name: job4 구현
        run: echo "job4의 내용을 구현해주세요"

  assign_reviewer:
    runs-on: ubuntu-latest
    steps:
      - name: job5 구현
        run: echo "job5의 내용을 구현해주세요"
